/**
 * Placeholder. GA-10 builds the real zero state here: starter questions and the
 * dataset named as provenance, and no computed result that is not downstream of a
 * question asked in this session (build-spec §1).
 */
export default function Page() {
  return (
    <main>
      <h1>Golden Analytics</h1>
    </main>
  );
}
